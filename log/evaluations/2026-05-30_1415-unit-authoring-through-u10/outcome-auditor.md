---
session: 2026-05-30_1415-unit-authoring-through-u10
reviewer: outcome-auditor
model_evaluated: "claude-opus-4-8"
grades:
  human:   { process: did-well, communication: did-well }
  claude:  { process: did-well, communication: did-well }
  overall: did-well
---
## Insights (ordered by significance)

1. **The U9 behavior-equivalence verifier is the standout artifact — genuinely sound, not just "done."** This is the kind of check that is easy to write badly (flaky, or with no real teeth) and Claude got the hard parts right. It materializes the reference program from the `start/u09-lab1` tag rather than trusting an in-tree copy, redacts wall-clock ISO timestamps with `TS.sub("<TS>", block)` so two runs don't falsely diverge, fixes the date battery *relative to today* (`PAST = date.today() - 400d`) so it's reproducible whenever a learner runs it, and exercises error paths (`bogus-cmd`) as behavior. The structural gate (`def_count >= 2` across ≥2 modules + a `taskflow.py < 0.7 * baseline` line check) won't be fooled by a trivial split. _Evidence:_ `course/labs/u09-lab1/verify.sh` lines 25–151; render lines 909–915. [claude] [process]

2. **Claude empirically proved the verifier's teeth instead of asserting them.** It didn't just say "this fails on scope creep" — it programmatically applied a real D1 overdue-fix to the refactored `domain.py`, ran the verifier, watched it report `BEHAVIOUR CHANGED`, then reverted and confirmed green again. It also ran the verifier on the *untouched* monolith first and confirmed it FAILs the "not split" gate (the negative case). This is the difference between a claim of correctness and a demonstrated one. _Evidence:_ render lines 993–1012 (FAIL on untouched monolith), 1405–1436 (temporarily applied D1 fix → verifier FAILs → reverted). [claude] [process]

3. **The open-loops audit caught a real defect Claude had itself introduced, and resisted a false alarm along the way.** Asked to "confirm all looks as it should," Claude cross-checked each loop against the actual repo and found three contradictory claims: a stale P4 placeholder `u09-lab1` SEEDED row, and a "legacy labs have no §2 row" assertion that was false and had propagated to four locations (L7, P5-U9-lab decision, tasks.md). Critically, when its first `grep` suggested "9 unverified keys" vs the documented "7," it did not blindly trust the discrepancy — it re-ran with `awk`, found grep had matched two comment lines, and confirmed the original "7" was correct. That is exactly the read-the-evidence-twice discipline an independent checker wants. _Evidence:_ render lines 5008–5074 (the 9-vs-7 reconciliation), 5107–5118 (the three real inconsistencies). [claude] [process]

4. **Claims in the conversation match the delivered files — verified end-to-end, not narrated.** Every "verified end-to-end (fails on start, passes on solution)" claim in this session is backed by an actual run shown in the transcript, not a hand-wave. The U9 solution smoke test confirmed D1/D2 are *preserved* (overdue empty, `--limit 3` prints 2 rows) and the verifier PASSed on the real solution branch. The `tools/_common.py` dogfooding artifact that U9's prose cites was confirmed to exist (`head -30`) before being referenced — no citing-from-memory. _Evidence:_ render lines 1377–1396 (smoke test + PASS), 831–851 (`_common.py` confirmed). [claude] [process]

5. **The U10 lab-type judgment is defensible and consistent, not a shortcut.** U10 ships no `start/`/`solution`/`verify.sh` — which could look like skipped work. But the reasoning is sound and precedented (U2/U4/U8): spec *quality* and "was the gate genuinely held" are judgment calls no script can grade (R7.AC3), so a prose rubric is the correct instrument; the verifiable part (a feature shipping green) is just the U5 loop, already covered. The choice of a deliberately-ambiguous feature (task dependencies — cycles, cross-project, blocked-completion) is well-targeted at forcing the design decisions a spec must surface. This is a sound product decision, not process theater. _Evidence:_ render lines 2343–2345, 2649–2652; decision P5-U10-lab at decisions.md. [claude] [process]

6. **The session's title undersells it — it ran "through U11" plus a substantial doc refactor and two audits.** The human's "push all and move onto U10," then "move to the next unit" (→U11), then a readability complaint, then two audit requests drove a wider scope than the slug implies. The doc-readability work (the 7,165-char single-table-cell in IMPLEMENTATION.md §3) was a real maintainability defect — that cell was duplicated verbatim in tasks.md, so the same wall of prose was being hand-edited in two places every unit. Claude diagnosed it with measurements (`awk` cell-width counts) rather than guessing, and the fix removed both the deformation and the duplication. _Evidence:_ render lines 4138–4219 (measured diagnosis), 4286 (the 7k cell). [human|claude] [process]

7. **One minor residue: a self-noted naming inconsistency that took a second turn to fully resolve.** Claude flagged that SEEDED.md §2 was titled "Primary per-lab defects" but had become a per-lab registry (it lists legacy, read-only, and feature-addition labs) — but initially *left the heading alone* "to avoid scope-creeping this audit," shipping a known-imperfect artifact. The human had to explicitly say "fix that title." This is small and the instinct (not scope-creeping an audit) is reasonable, but a stricter outcome standard would have either fixed it in-band or asked rather than shipping the mismatch. To Claude's credit, when told, it fixed the title, intro, the stale `(planned)` label, and a downstream decisions.md L4 note, and correctly declined to churn three other references that were accurately scoped. _Evidence:_ render lines 5307 (left it alone), 5311 (human prompt), 5446–5448 (declined the churn with a reason). [claude] [process]

8. **The human's process is a model of how to drive a build to sound output.** The "review the open loops and confirm all looks as it should" request is precisely the independent-check instinct this lens prizes — it surfaced the SEEDED drift that would otherwise have rotted. Approvals were per-change and explicit ("push all," "Go ahead and fix that title... for a new commit"), matching the repo's standing rule against inferred blanket permission. The readability complaint was specific and actionable. _Evidence:_ render lines 2130, 4138, 4842, 5311. [human] [comms]

## What worked / what didn't (both parties)

**Claude — strengths.** The work product held up. The U9 verifier is the best evidence: a behavior-equivalence harness with the non-determinism traps (timestamps, wall-clock dates) actually handled, proven by adversarial testing rather than assertion. Every "verified" claim in the transcript is backed by a shown run. The audit work was the inverse of process theater — it found a real, self-introduced defect and a false-positive both, and it re-checked a suspicious count instead of trusting the first tool. Communication was tight: each commit message accurately describes the diff, and the closing summaries distinguish "what changed" from "what I deliberately left alone, and why."

**Claude — weaknesses.** Thin. The only real one is shipping the SEEDED §2 title/registry mismatch as a known imperfection and deferring it rather than resolving it in-band — the human had to ask. Minor, and the underlying instinct was sound.

**Human — strengths.** Drove the session toward verifiable soundness: the open-loops audit request is exactly what catches drift before it compounds, and the readability complaint targeted a genuine maintainability defect. Approvals were explicit and per-change.

**Human — weaknesses.** Almost none for this lens. If anything, the U10/U11 prose-self-check labs (no automated verifier) accumulate unverifiable surface area in the course; the human accepted that without pushing on it — but the rationale is documented and consistent, so this is a watch-item, not a fault.

## Bottom line

A high-integrity build session whose artifacts would survive an independent check. The marquee deliverable — the U9 behavior-equivalence verifier — is correct in the ways that matter (baseline from tag, timestamp redaction, today-relative dates, real structural teeth) and was *proven* sound by adversarial testing, not narrated as done. The open-loops audit then did genuine quality work: it found a stale placeholder and a false claim that had spread to four files, while correctly refusing to overreact to a grep miscount. The one blemish — deferring a known title/registry mismatch until prompted — is small against a session where the process consistently produced claim-matching, defect-free output. Quality here was real.
