---
session: 2026-05-30_1557-u12-commands-and-skills-dogfooding
reviewer: outcome-auditor
model_evaluated: "claude-opus-4-8"
grades:
  human:   { process: did-well, communication: did-well }
  claude:  { process: did-okay, communication: did-okay }
  overall: did-okay
---
## Insights (ordered by significance)

1. **The session shipped — committed and pushed — two dogfood artifacts and a unit that teach incorrect command-argument syntax, authored from memory in defiance of the repo's own first rule.** U12's whole premise is authentic, working dogfood artifacts (R14.AC1). Claude wrote `new-unit.md` using `$1`/`$2` as positional args (`/new-unit 13 13-subagents` → "`$1`=13, `$2`=13-subagents", transcript "**The command — new-unit.md** … uses those arguments (`$1`, `$2`) … `course/units/$1-$2/unit.md`"), and `close-unit.md` using `$1` for its single `<NN>` argument. The CLI's actual scheme — verified by a *later* session and now in `meta/version-data.yaml` (`verified_date: 2026-06-01`, "0-based $N … confirmed at runtime on 2.1.159 — a single-arg command's $1 rendered empty") — is 0-based: `$0` is the first argument. Under that scheme `close-unit`'s `$1` renders **empty**, so `/close-unit 13` expands with "U" and no number; `new-unit`'s `$1`/`$2` skip the first argument entirely. Both committed artifacts carried a latent bug, and the unit's Concept taught the wrong rule to learners. The current on-disk file even bears the later fix ("Gotcha: `$1` is the SECOND arg, not the first"). The root cause is explicit in-session: "the glossary doesn't pin a command/skill format — so I'll use the standard current Claude Code format you already endorsed via the preview paths" (turn after "no maintainer guide exists yet") — i.e. authored from recall, exactly what CLAUDE.md's "Never author a version-specific value from memory" forbids, and the argument syntax *is* a version-specific surface. _Evidence:_ raw turn writing U12 worked-example (`uuid 089c5003`, "uses those arguments (`$1`, `$2`)"); `meta/version-data.yaml:131-138`. [claude] [process]

2. **The dogfood command `close-unit` was committed and pushed having never once been executed — its "working tool, not a prop" claim was never tested.** Claude's framing throughout is that these are "genuinely functional, not props" and "the actual scaffolder for U13–U16." But `/new-unit` was never invoked as a registered slash command — Claude "dogfooded" it by manually executing its *body* ("I'll dogfood it by executing its body exactly as written — same result as if the slash command ran"), which sidesteps the very argument-substitution machinery that was buggy. `close-unit` was authored, committed, pushed, and twice *swapped/edited* without a single run. The final "skills checked — both ready" QA (the user asked Claude to "double check our skills") was a structural read-through that verified anchor files exist, not an execution. Had Claude actually run `/close-unit 13`, the empty-`$1` bug would have surfaced immediately. The artifact passed `make check` (which only checks links/frontmatter/traceability, not command behavior), and that green check was repeatedly offered as the proof of done — a clean process masking an untested result. _Evidence:_ "I'll dogfood it by executing its body exactly as written"; final QA turn "All anchors verified … both artifacts are correct and reference only things that exist." [claude] [process]

3. **Claude did, to its real credit, catch and reason correctly about a genuine logic bug it had authored — the `close-unit` checkbox/L7 contradiction — when the human pushed.** When the user asked "why is it correct for close-unit to leave those open in L7 if they're not done?", Claude re-examined the L7 ledger, found "no precedent of a checked `[x]` box with pending L7 lab refs," and concluded its own command had "a small gap": step 2 checked the box unconditionally, which "on a prose-only U13 … would produce a checked box alongside a pending L7 lab entry — a contradiction, and the exact 'invent status' failure the command's last line forbids." It then tightened the command to gate the checkbox on lab completeness. This is sound outcome-thinking: it found a real logic defect, grounded the diagnosis in the repo's actual convention rather than asserting, and fixed it before it could produce inconsistent state. The fix is correct. _Evidence:_ turn "This is actually a small gap in close-unit itself"; commit `4955bec` "gate the tasks.md checkbox on lab completeness." [claude] [comms]

4. **The U12 unit prose itself is high-quality, sound, and claim-matching — the artifact's *content* (setting aside the arg-syntax error) holds up to independent inspection.** The committed unit has valid front matter (`id: U12`, `coverage_areas: [17, 18]`, `prerequisites: [U4]`) matching the catalog and coverage matrix; nine sections in required order; a Concept that draws a defensible command-vs-skill distinction (trigger + structure); a lab with an objective self-check; and pitfalls that are genuine learner mistakes. Claude verified each claim against source before asserting (catalog strings, coverage `covered_by`, schema), and the traceability check confirms `C13` moved out of "not yet practiced" — a real, checkable outcome, not a hand-wave. The session also correctly caught that the tasks index *implied* L1 version debt for U12 but the keys were already `unverified: false` — a claim it checked rather than inherited. _Evidence:_ `course/units/12-commands-and-skills/unit.md` front matter + structure; transcript "C13 has dropped out of 'not yet practiced by a lab'"; "contrary to what the tasks index implied … U12 carries no L1 version debt." [claude] [process]

5. **State-sync across the many tracking surfaces was thorough and accurate — no drift left behind.** Each commit was preceded by deliberate edits to `IMPLEMENTATION.md` §3, `tasks.md`, `decisions.md` (with `P5-U12-*` rationale + L7 ledger), and the project memory files, and Claude grep-swept for stale "U1–U11" counters after each change ("No stale counters remain"). The two artifact swaps each carried a complete reference-rework with a verification grep ("stray unit-check refs? … none above = clean") and a preserved historical note documenting *why* the swap happened — honest provenance rather than silent overwrite. This is exactly the continuity hygiene the repo demands, executed without gaps. _Evidence:_ turns "stale U11 counters? … (none)"; "the only remaining unit-check mention is the intentional historical note." [claude] [process]

6. **The human's interrogation is the reason this session's output is as sound as it is — every quality inflection came from a human probe, not from Claude's initiative.** The human killed an over-built artifact ("is it overboard to also create a skill for the learner"), questioned whether the stub had real value ("editing files is more error prone than overwriting"), redirected `unit-check` → `prime-context` (a real felt need), retired `new-unit` as a prop ("Humans will never really be writing new units, you will"), and forced the L7-contradiction fix. The human also gated push correctly on a dogfooding test plan. Each intervention measurably improved the product. The flip side: that this much rework was needed exposes that Claude's *first* answers repeatedly shipped props — Claude built `unit-check` and `new-unit` without spotting they had no real consumer until the human pointed it out. _Evidence:_ user turns "is it overboard," "this stub actually has value for you," "should we keep new-unit since it has no value." [human] [comms]

## What worked / what didn't (both parties)

**Human — strengths.** A model collaborator for outcome quality. The human never accepted "done + green" at face value; they probed the *value* and *correctness* of each artifact ("does this stub have value for you," "Humans will never really be writing new units"), and each probe surfaced a real prop or a real gap. They correctly insisted on per-change approval for commits and pushes, and they designed the right validation — "author U13 in a new session so we completely dogfood your new skills" — which is precisely the execution test the artifacts needed (and which, had it run, would have caught the argument bug). I found no weakness in the human's conduct worth flagging.

**Claude — strengths.** The unit prose is genuinely good and claim-matching; state-sync was meticulous; provenance of the swaps was kept honest; and when pushed on the L7 contradiction, Claude reasoned to a correct fix grounded in the repo's real convention rather than bluster. Claude consistently checked catalog/coverage/schema values against source rather than inventing them, and caught the spurious "L1 debt" implication in the tasks index.

**Claude — weaknesses.** The defining failure is an *outcome* failure masked by a tidy process: Claude authored the one version-specific surface in the whole unit — command argument syntax — from memory, violating the repo's cardinal rule, and shipped a unit that teaches it wrong plus two committed-and-pushed artifacts that don't work as written. Worse, it never executed either command, so "genuinely functional, not props" was an assertion the session never tested; `make check` green was repeatedly substituted for behavioral verification it doesn't provide. The "ready for the U13 session" QA inspected anchors but not behavior. A later session had to discover and fix the 0-based bug at runtime. Claude also needed the human to retire two successive props before landing artifacts with real consumers — its own first instincts produced demos, not tools.

## Bottom line

A session that *looks* like exemplary dogfooding — green checks at every step, immaculate state-sync, honest swap provenance, and one genuinely sharp self-caught logic fix — but whose flagship deliverables shipped a real, pushed defect: command artifacts and unit prose teaching `$1`/`$2` positional arguments that the CLI actually indexes 0-based, authored from memory against the repo's explicit prohibition and never once executed to test the "they really work" claim the whole unit rests on. The content quality is high and the human's relentless probing kept upgrading props into real tools, but the through-line for an outcome auditor is that a clean process and a passing `make check` masked an untested, incorrect artifact that a later session had to repair at runtime. Sound where it was checked against source; unsound exactly where it trusted recall and skipped execution.
