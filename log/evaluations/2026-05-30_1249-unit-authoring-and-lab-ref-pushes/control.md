---
session: 2026-05-30_1249-unit-authoring-and-lab-ref-pushes
reviewer: control
model_evaluated: "claude-opus-4-8"
grades:
  human:   { process: did-well, communication: did-well }
  claude:  { process: did-well, communication: did-well }
  overall: did-well
---
## Insights (ordered by significance)

1. **Claude built and tested the feature in the working tree *before* carving the git refs, then proved the verifier gates both ways.** Rather than author a verifier and hope, Claude implemented `GET /projects/{id}/stats` end-to-end, ran pytest green (39, up from 36), stashed the feature to recover a clean start state, confirmed `verify-lab` *fails* on the clean tree (404) and *passes* on the solution, and re-confirmed the full `reset → fail-clean → apply-solution → pass` loop after committing. This is the right order of operations for authoring a lab whose whole value is that its gate is real. _Evidence:_ "confirm the verifier correctly **fails** on the clean start state" → `verify exit=1 (expect 1)` / `verify exit=0 (expect 0)`. [claude] [process]

2. **Claude correctly refused to infer push permission, but the human had to grant it explicitly each time.** Claude committed during the unit work, then stopped and presented — "**nothing pushed** (per your standing preference)." The human then said "commit and push," and later "push start/u01-lab1 too." Claude pushed only what was sanctioned and verified each push landed (`git ls-remote --tags`). This matches the project's per-change approval rule and shows the discipline working as intended. _Evidence:_ session close "Two commits ... **nothing pushed**"; human turn "commit and push." [claude] [process]

3. **A real environment break was handled cleanly and turned into a durable memory fix — with one over-correction the human had to walk back.** `python: command not found` surfaced because the wrong venv was active; the human interrupted, supplied the correct path, and Claude recovered. Claude then proactively updated the project memory note about the venv location. But its first edit asserted the venv is "not always pre-activated," which the human corrected: it *should* be active by default, only re-activate if you detect it isn't. Claude amended both the note and the index line accordingly. Good instinct to capture the lesson; the initial framing slightly overgeneralized from a one-off human oversight. _Evidence:_ "I have forgot to activate the proper venv"; later human turn "the venv should be activated by default and to only activate it if you detect it hasn't been activated." [claude] [process]

4. **State-keeping was thorough and consistent with the project's own continuity discipline.** Claude updated IMPLEMENTATION.md §3, the `tasks.md` status, `decisions.md` (new P5-U5 rows + the L7 open-loop), `SEEDED.md` §2, *and* the cross-session memory index — and explicitly noted U5 added **no new version-verification debt** because it consumed only already-verified `{{vd:plan-mode}}`/`{{vd:thinking}}` keys. This is exactly the "keep the ledger current" obligation the repo demands, executed without being asked. _Evidence:_ commit body "State updated: IMPLEMENTATION §3, tasks.md, decisions.md (P5-U5-lab, P5-U5-vd; L7), SEEDED.md §2." [claude] [process]

5. **The human's terse, high-trust steering worked because Claude reliably grounded itself first.** "continue with the next unit" was the entire brief, yet Claude read IMPLEMENTATION.md §3, the P5 task table, prior units (U1, U4), the workflow definition, and the codebase before writing anything — landing on the correct next unit (U5) and a feature that fits the codebase's existing service-layer conventions. The human could stay terse precisely because the agent did the orientation. _Evidence:_ opening "Let me check the current state before diving in" → reads §3, P5-units, U1/U4, workflows, models. [human] [communication]

6. **Claude surfaced a small, real inconsistency rather than silently "fixing" it — and offered options.** It noticed it had pushed `start/u05-lab1` while `start/u01-lab1` was (it believed) local-only, flagged the asymmetry, and asked whether to keep tags local or push U1's to match. The human chose to match. This is good judgment about what to escalate versus decide unilaterally. _Evidence:_ "Let me know if you'd prefer the tags kept local-only ... or want to push `start/u01-lab1` too." [claude] [communication]

## What worked / what didn't (both parties)

**Claude (process).** Strong throughout. The defining move was building the reference implementation first and validating the lab's verifier empirically (fail-clean / pass-on-solution) instead of trusting it by construction. The stash → tag → branch → pop choreography to separate a clean start state from the reference solution was handled carefully, including catching that `git checkout -- path` had staged files and restoring with `git restore --staged --worktree`. Commit hygiene was good (explicit `git add` of named paths to avoid sweeping in the stray `.vscode/`; pre-commit `make check` green each time). The one blemish is the `python: command not found` detour — but that was a host-env misconfiguration the human owned, not a Claude error, and Claude recovered in one step once given the path. Minor: a couple of Edit-before-Read misfires (project.py, `__init__.py`, SEEDED.md, MEMORY.md) cost a few wasted tool calls, though the harness caught them harmlessly.

**Claude (communication).** Clear and appropriately scoped. Each phase was narrated with a one-line rationale ("confirm the verifier correctly fails on the clean start state"), the closing summary was accurate and honored "nothing pushed," and the `.vscode/` housekeeping note was a genuinely useful flag rather than noise. The venv memory note's first framing over-generalized, but Claude accepted the correction precisely.

**Human (process).** The human ran this as a high-trust, tightly-gated session: a one-line kickoff, an interrupt the moment something looked wrong (the venv), and explicit, per-action authorization for every push. They caught Claude's over-broad memory claim and corrected it crisply. This is effective delegation against a well-instrumented repo — the kind of supervision that lets terse prompts work.

**Human (communication).** Concise and unambiguous. "continue with the next unit," "commit and push," "push start/u01-lab1 too to match" — each instruction was actionable without round-trips, and the venv correction included the exact path. The one correction that required a follow-up (the venv-activation framing) was stated clearly enough that Claude fixed it in one pass.

## Bottom line

A clean, well-instrumented authoring session: from a one-line prompt, Claude oriented itself against the spec's own state files, authored U5 and its lab, and — most importantly — *empirically proved* the lab's verifier gates correctly before publishing, while keeping every continuity artifact (ledger, §3, SEEDED, memory) in sync. The human supervised with terse, high-trust steering and intervened decisively where it mattered (a misactivated venv, an over-broad memory note, and per-change push approval). The push discipline held: nothing left the working tree without explicit go-ahead. The only friction was an environment misconfiguration the human owned and a handful of harmless Edit-before-Read retries — neither of which dented the outcome.
