---
session: 2026-05-30_1249-unit-authoring-and-lab-ref-pushes
reviewer: outcome-auditor
model_evaluated: "claude-opus-4-8"
grades:
  human:   { process: did-well, communication: did-well }
  claude:  { process: did-well, communication: did-okay }
  overall: did-well
---
## Insights (ordered by significance)

1. **The lab apparatus is genuinely sound, not "completed" — it survives an independent check because Claude built one into the session.** The strongest outcome here is `u05-lab1/verify.sh`: it spins up its own isolated in-memory DB mirroring `conftest.py`, seeds known counts, and asserts the full contract (per-status counts, zero-fill on an empty project, ownership-404 for a non-owner) without trusting the learner's file structure. Claude then proved the gate both directions: it fails on the clean start state (`AssertionError: GET /projects/{id}/stats not implemented … HTTP 404`) and passes once the solution is applied. That fail-clean/pass-solution demonstration is exactly the independent check my lens demands, and it was run, not asserted. _Evidence:_ turns "=== 2. verify-lab on start state (should FAIL …) === verify exit=1 (expect 1)" and "=== 3. apply solution … verify exit=0 (expect 0) … u05-lab1 PASSED". [claude] [process]

2. **The feature implementation is correct and the suite count moved as claimed.** The reference implementation (ProjectStats schema + `project_task_stats` service enforcing ownership via `get_project` + thin route + tests) took the suite from 36 to a verified `39 passed`, and Claude built and ran it in the working tree *before* structuring commits — so the "passes" claim is backed by an actual pytest run, not a forecast. The service correctly threads through the codebase's existing layering convention (domain logic in `services/`, router thin), which is the exact convention the unit's worked example and pitfalls section teach. Claims match the delivered artifact. _Evidence:_ "The reference implementation passes (39 tests green, was 36)"; later `39 passed, 1 warning`. [claude] [process]

3. **State-surface bookkeeping is accurate and matches what shipped — no drift, no over-claiming.** IMPLEMENTATION.md §3, tasks.md, decisions.md (new P5-U5 rows), SEEDED.md §2, and the cross-session memory index were all updated, and the `make check` traceability output independently corroborated the headline claim ("C6 now lab-traced") — C6 dropped off the "can-do(s) not yet practiced by a lab" PEND list once the lab landed. The closing summary's claims ("C6 dropped off the pending list", "no new version-verification debt" because U5 uses only the *verified* `plan-mode`/`thinking` keys) are each checkable against transcript evidence and hold. _Evidence:_ `make check` output "can-do(s) not yet practiced by a lab: ['C10','C11','C12','C13','C14','C15','C16','C17','C7','C8','C9']" (C6 absent); decisions ledger shows `plan-mode`/`thinking` as `unverified: false`. [claude] [process]

4. **The version-currency discipline held under its own rules, but the on-disk file shows a later session reworked this artifact — so judge by the transcript.** This session authored `unit.md` directly against verified `{{vd:plan-mode}}`/`{{vd:thinking}}` keys and correctly added *no* new L1 debt. (Note for the record: the file on disk now carries a `GENERATED from unit.src.md` banner and version `2.1.159` — that is a *later* render-pipeline/refresh session's work, not this one's; this session targeted the 2.1.158-verified keys. I graded the session's artifact, not the post-edited file.) Within the session, the version handling is clean and honest. _Evidence:_ session summary "both using *verified* `{{vd:plan-mode}}`/`{{vd:thinking}}` keys, so **no new version-verification debt**". [claude] [process]

5. **The push went one step past what the user enumerated — defensible, but it tested the per-change-approval rule.** The user said "commit and push." Claude committed the `.gitignore` change and then pushed three refs: the branch, plus the new `solution/u05-lab1` *branch* and `start/u05-lab1` *tag*. The branch+solution were reasonable (mirroring the existing `solution/u01-lab1` remote layout), and Claude did surface the asymmetry afterward rather than hide it ("slightly more complete than U1's setup … Let me know if you'd prefer the tags kept local-only"). But project memory is explicit that "push" approval is per-change and tags/branches are not blanket-authorized by a single "push." Pushing the tag/solution under a "push" that most naturally referred to the just-made commit is a mild over-read; it landed fine here only because the user then ratified it. _Evidence:_ user "commit and push"; Claude's three-way `git push origin spec/tasks-phase` / `solution/u05-lab1` / `start/u05-lab1`; CLAUDE.md "Approval is per-change, not standing … an explicit 'push' before pushing." [claude] [process]

6. **A self-inflicted memory churn produced a transiently *wrong* artifact that the user had to correct.** Claude proactively edited the cross-session memory note to say the venv was "not always pre-activated," generalizing from a single missed-activation that the user immediately explained was *his* oversight (he forgot to activate before starting). The user then had to correct the memory to its true state ("the venv **should be active by default** … only activate if you detect it hasn't been"). The note Claude wrote was a claim that didn't match reality and would have mis-steered future sessions had the user not caught it. Minor in blast radius, but it is precisely the "plausible artifact, wrong content" my lens flags. _Evidence:_ Claude's edit "venv … (not always pre-activated)"; user "the venv should be activated by default and to only activate it if you detect it hasn't been"; Claude's correction edit "should be active by default." [claude] [comms]

7. **The human's mid-session catch and tight directives kept the outcome clean.** The author interrupted the wrong-venv flailing with the exact fix (`source /home/jj/venvs/main/bin/activate`), unblocking the whole verification chain, and his later instructions were precise and outcome-shaped ("add `.vscode` to `.gitignore` … then commit and push", "push `start/u01-lab1` too to match"). This is low-overhead, high-signal steering that let Claude do verifiable work. _Evidence:_ "I have forgot to activate the proper venv … Please activate … `source /home/jj/venvs/main/bin/activate`"; "push start/u01-lab1 too to match". [human] [comms]

## What worked / what didn't (both parties)

**Claude — strengths.** This is a model "outcome" session: the headline deliverable (a working-tree-graded feature lab) was built, tested, and *proven both directions* inside the session, so an independent auditor doesn't have to take anything on faith. The verifier is real verification, not ceremony — isolated DB, contract-level assertions, ownership edge included as the woven CV/security gate. The feature respects the codebase's load-bearing layering convention, the suite count claim is backed by an actual run, and every state-surface update is corroborated by the `make check` output rather than merely asserted. Commit hygiene (stash the feature → tag the clean start → build the solution branch from the stash → verify end-to-end → restore) is disciplined and correct.

**Claude — weaknesses.** Two soft spots, both in the comms/judgment register rather than the core artifact. First, the push reached past the literal change the user named, leaning on a precedent rather than asking — it worked out because the user ratified it, but the project's per-change rule exists precisely so that doesn't become habit. Second, the proactive memory edit manufactured a *false* note ("not always pre-activated") from a one-off the user owned, which the user had to reverse. Also worth noting for cleanliness: a handful of Edit calls hit "File has not been read yet" and had to be retried — harmless to the outcome, but avoidable friction.

**Human — strengths.** Decisive, well-targeted interventions. The venv correction was the unblock the entire verification chain depended on, and the closing directives were crisp and outcome-oriented. He also caught and reversed the bad memory note, which is the kind of oversight that keeps future-session quality from quietly degrading.

**Human — weaknesses.** Little to fault on outcome grounds. The one structural thing he let pass without comment was the slightly-broad push (tag + solution branch under a single "push"); endorsing it after the fact is fine, but it does soften the per-change-approval norm his own project memory codifies.

## Bottom line

A clean, verifiable build session whose artifact would survive an independent check — because Claude built that check into the session and ran it both directions. The U5 unit, lab, verifier, and reference solution are correct, contract-matching, and backed by actual test runs, and every "done" claim in the wrap-up is corroborated by transcript evidence rather than asserted. The only blemishes are in judgment, not work product: a push that crept one step past the named change, and a proactively-written memory note that stated something untrue until the user corrected it. Neither touched the shipped code. Strong outcome, lightly imperfect process.
