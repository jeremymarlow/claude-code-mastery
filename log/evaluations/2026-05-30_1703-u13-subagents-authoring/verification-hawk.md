---
session: 2026-05-30_1703-u13-subagents-authoring
reviewer: verification-hawk
model_evaluated: "claude-opus-4-8"
grades:
  human:   { process: did-well, communication: did-okay }
  claude:  { process: did-well, communication: did-well }
  overall: did-well
---
## Insights (ordered by significance)

1. **Claude verified the version-specific CLI surface against the live tool before authoring a word of it — the cardinal rule of this repo, honored.** Before writing the subagents unit, Claude ran `claude --help 2>&1 | grep -i -A1 "agent"` and `claude agents --help` and read the actual output, rather than trusting either its own training or the existing `meta/version-data.yaml` value. The help output confirmed the exact shape it then taught: `--agent <agent>`, `--agents <json>` with a `description` field (`'{"reviewer": {"description": "Reviews...'`), and the `agents` subcommand. Its summary cited the evidence precisely — "CLI surface verified: `--agent <agent>` (select), `--agents <json>` (inline custom agents, with a `description` field)" (turn after the `--help` run, ~L548). This is verify-before-acting done right: the worked example's `--agents <json>` shape is anchored to read help output, not memory. _Evidence:_ Bash `claude --help ... grep -i -A1 "agent"` and `claude agents --help` (~L491-541); "CLI surface verified" (~L548). [claude] [process]

2. **Claude distinguished a "PEND is expected" from a "PEND is a regression" instead of treating green as proof.** When `make check` returned green with three PENDs (`R8`, `C15–C17` labs, all rubric dimensions), Claude did not stop at "all checks passed." It read the residual list and reasoned about each: "C14 dropped out of the 'not yet practiced by a lab' list (only C15–C17 remain, which are U14–U16)" and later "the 3 PENDs are expected downstream work: R8 + C15–C17 labs land in U14–U16, and the rubric dimensions are P6 capstone" (~L995, ~L1226, ~L1357). That is exactly the discipline the repo's own working agreement warns about — plain `make check` can stay green over a real regression — and Claude verified the *content* of the green, not just its color. The one gap (see #5) is that it never ran `make check-strict`, the repo's documented Definition-of-Done gate. _Evidence:_ `make check` output (~L982-988); "C14 dropped out" (~L995). [claude] [process]

3. **Claude treated the source files as truth over its own prior summary — re-verifying the `subagents` key was `unverified: false` rather than asserting it.** Rather than claiming "no L1 debt" from memory, it grepped the key's state: first reading `meta/version-data.yaml` (`unverified: false`, ~L401-403), then on the duplicate `/close-unit` pass it re-ran `grep -A6 "^  subagents:" meta/version-data.yaml | grep "unverified:"` and confirmed `unverified: false` live before declaring version currency done (~L1329-1336). Even the JSON twin's sync was driven by an actual `grep -c "subagents" meta/version-data.json` to confirm presence, and it checked whether any tool even consumes the JSON before bothering ("The JSON twin isn't consumed by any tool (render-vd reads the YAML)", ~L714). Claims were checked, not asserted. _Evidence:_ ~L401-403, ~L684-714, ~L1329-1336. [claude] [process]

4. **The duplicate `/close-unit` was handled by verification, not blind re-execution — a textbook "confirm state before acting" moment.** When the human re-fired `/close-unit` with no argument after Claude had already run every close-unit step in the prior turn, Claude did not re-do the edits (which would have duplicated decision entries). It said "Rather than duplicate entries, let me verify each state file is already in sync" and then ran a battery of greps confirming the §3 marker, the `[x] **U13**` box, the `P5-U13` entry count (4), the L7 ledger line, and the vd key — each backed by a real read (~L1282-1317). This is precisely the lens's "verify before acting": confirm the current state before deciding what action is needed. _Evidence:_ "Rather than duplicate entries, let me verify..." (~L1282); confirmation greps (~L1288-1317). [claude] [process]

5. **The one real lapse: "done" was declared and the unit committed/pushed on plain `make check`, never `make check-strict` — the repo's own stated Definition-of-Done gate.** CLAUDE.md is explicit that "done" gates on strict, and that plain `make check` reporting `PEND` "can stay green while a real regression lands." Claude proved green twice with plain `make check` and reasoned the PENDs away as downstream, which is defensible mid-P5 — but it never confirmed that the strict gate behaves as expected for this unit, and the human's "commit and push" was honored without anyone running strict. The repo's own convention treats strict as the proof of "done"; here "fully closed out" (~L1226) rested on the non-strict signal. This is a small but on-lens gap: the strongest available verification was left unrun. _Evidence:_ both check runs are plain `make check` (~L953, ~L1330); CLAUDE.md's strict gate is never invoked in the session. [claude] [process]

6. **The human's verification contribution was the dogfooded scaffolding and an honest commit-gate — but the duplicate `/close-unit` and a bare "commit and push" leaned on Claude to supply the rigor.** The human's `/prime-context` and `/close-unit` commands are themselves verification machinery (read order, state-sync, "Do not invent status... say so and leave it open"), and that scaffolding visibly forced Claude to check rather than assert. The human also correctly withheld push authority until an explicit "commit and push this" (~L1365), honoring the repo's per-change-approval rule. The weaker note: the human fired `/close-unit` with no unit number and then authorized commit+push in one breath without independently confirming the diff or asking for strict — delegating the verification burden entirely to Claude. It worked because Claude was disciplined, but the human's own verify-the-artifact step was light. _Evidence:_ `/close-unit` with empty arg (~L1255); "commit and push this, and start U14" (~L1365). [human] [process/comms]

7. **Claude's reporting was evidence-anchored and free of fabricated specifics — uncertainty and scope were stated honestly.** Every "done" in the wrap-ups is tied to a shown check: version key "re-verified live this session: `--agent` / `--agents <json>` / `agents` subcommand," the lab classified honestly as "genuinely a no-refs prose-self-check... so no L7 lab refs are left open" (~L1356-1359). It explicitly flagged what it had *not* consumed ("L1 — 7 unverified keys, none consumed here", ~L1244) rather than implying total resolution. No version number was authored from memory; the `2.1.158` and provenance came from the file and the live `--help`. _Evidence:_ ~L1356-1359; ~L1244. [claude] [comms]

## What worked / what didn't (both parties)

**Claude — strong.** This is a near-model session for the verification lens. The version-specific surface (the whole point of a subagents unit) was confirmed against `claude --help` and `claude agents --help` and read, not trusted; the taught `--agents <json>` shape traces directly to that output. Green suites were interrogated, not believed — Claude consistently separated expected downstream PENDs from regressions and named which units would clear each. The duplicate `/close-unit` was resolved by reading state rather than re-writing it. Claims in the summaries were each backed by a grep or a check, and scope of what remained unverified (the 7 L1 keys) was stated plainly. The single weakness is the absence of `make check-strict` — the repo's own documented "done" gate — before declaring the unit closed and pushing; the proof of done rested on the weaker signal.

**Human — solid scaffolding, lighter personal verification.** The human's real contribution to verification is structural: the `/prime-context` and `/close-unit` skills encode "trust the files, not memory" and "do not invent status," and they demonstrably pulled Claude toward checking. The push gate was honored correctly (explicit authorization required and given). What's lighter: the human fired `/close-unit` with no argument (a small slip Claude absorbed gracefully), and authorized commit+push without independently reading the diff or asking for the strict gate — the verification burden was effectively delegated. Communication was terse and effective ("go ahead with U13"; "commit and push this, and start U14") but offered no independent confirmation step of its own.

## Bottom line

A disciplined, evidence-first build session. Claude honored the cardinal rule — it verified the subagent CLI surface against the live tool before authoring anything version-specific, read the artifact rather than trusting memory, interrogated green checks instead of resting on their color, and resolved a duplicate command by confirming state rather than blindly re-running. The human's dogfooded scaffolding deserves credit for institutionalizing that rigor. The lone on-lens gap is that "done" and the push rode on plain `make check`, never the repo's own stricter Definition-of-Done gate — the strongest available proof was left unrun.
