---
session: 2026-05-31_1813-version-refresh-l9-and-r8-strict-gate
reviewer: control
model_evaluated: "claude-opus-4-8"
grades:
  human:   { process: did-well, communication: did-well }
  claude:  { process: did-well, communication: did-well }
  overall: did-well
---
## Insights (ordered by significance)

1. **Claude refused to fabricate provenance under a tempting shortcut, and said so out loud.** When bumping `_verified_version` to 2.1.159, Claude deliberately left the *per-key* `verified_version: 2.1.158` and dates untouched, reasoning that it had not actually re-run the in-REPL `/help` checks this session, so stamping today's date on them "would fabricate provenance (R12.AC3)." It bumped only the overall content stamp, which the byte-stable reference + changelog genuinely support. This is the single highest-value behavior in the session: the cheap move (bump everything, call it refreshed) was right there and got refused on a stated honesty rule. _Evidence:_ "Leave per-key `verified_version: 2.1.158` untouched… bumping their `verified_date` to today would fabricate provenance (R12.AC3)." [claude] [process]

2. **An unrelated investigation surfaced a silent, pre-existing regression and Claude proved it was not its own doing.** During the refresh, `make check-strict` failed on an `R8` PEND. Rather than ignore it (it was outside the task) or absorb blame for it, Claude stashed its own edits and re-ran strict on committed HEAD to prove the failure predated the session, then root-caused it to P7 slice `cfd04ec` stripping the only `R8` references during a prose de-coding sweep. It correctly explained *why nobody caught it*: P7 gated on plain `make check`, where an unreferenced requirement is a non-failing PEND. _Evidence:_ "Confirmed **pre-existing**… stashed my edits and re-ran" → root-cause to `cfd04ec`. [claude] [process]

3. **The fix targeted the systemic gap, not just the symptom — at the human's prompting.** The human didn't just ask to patch R8; they asked to make `close-unit`/CLAUDE.md gate "done" on strict so the class of bug can't silently recur. Claude implemented the R8 anchor as a non-rendered HTML comment (matching the existing unit-template convention, honoring P7's learner-prose intent) AND moved the confirm-gate to strict, then flagged a remaining asymmetry (pre-commit/CI still non-strict) for the human's judgment rather than silently deciding it. _Evidence:_ "left the **pre-commit hook and CI on non-strict**… If you'd rather have the pre-commit hook itself enforce strict… say so." [claude] [process]

4. **The human supplied the one piece of context Claude could not derive, by interrupting at the right moment.** Claude was scoping the refresh from files alone and starting to treat 2.1.159 as a routine drift. The human interrupted to explain the deliberate setup: the CLI reference was *generated against 2.1.159* during P8, and 2.1.158 was held as the recorded anchor only to keep the upgrade trigger armed until the feature shipped. This converted an ambiguous "what changed and what must I re-verify" into a clean "disarm the trigger" refresh — and Claude immediately re-scoped correctly. _Evidence:_ "I stopped you to give you extra context from before. The tool to create the cli reference was actually made after the tool had updated itself to 2.1.159." [human] [communication]

5. **Disciplined commit hygiene on both sides: per-change approval, never inferred.** The human approved each commit/push explicitly ("commit and push the refresh", "commit and push; /capture-session"), and Claude consistently stopped at "done, not committed — want me to commit?" without inferring standing permission from the prior rhythm. This matches the CLAUDE.md per-change-approval rule exactly across four separate commit points. _Evidence:_ repeated "changed files… **not yet committed**. Want me to commit?" pattern. [claude] [process]

6. **Mechanical-honesty attention to the JSON twin diff.** Claude noticed its regenerated `version-data.json` had unescaped `—` sequences (it used `ensure_ascii=False`), creating spurious diff noise, and re-ran with `ensure_ascii=True` to match the original encoding so the diff was *only* the two stamp lines. Small, but it reflects a habit of keeping diffs reviewable and not leaving unexplained churn. _Evidence:_ "The original twin was generated with `ensure_ascii=True`… my regen unescaped them, creating noise." [claude] [process]

## What worked / what didn't (both parties)

**Human — process.** The human ran this as a tight, well-sequenced session: prime context, fix the stale §3 line, review open loops, strike a settled one, then perform the refresh, then chase the strict-gate finding to a durable fix. Each step was a small reviewable slice with an explicit gate. The standout is the mid-stream interruption to inject the P8 trigger-arming context — that is the kind of knowledge that lives only in the human's head and that no amount of file-reading recovers; surfacing it at exactly the moment Claude needed it prevented a misframed refresh. The human also escalated the R8 fix from "patch it" to "fix the class of bug" (gate done on strict), which is the more valuable instinct.

**Human — communication.** Terse but unambiguous. Commands like "fix the stale §3 P8-merge line", "Strike L6 as settled for v1", "investigate the R8 strict-gate failure" are precise and scoped. The interruption message was clear and complete. The one place a reader might want more was the final composite instruction ("update the skill or CLAUDE.md, where appropriate… I believe there's one already for make check") — slightly hedged — but Claude resolved it correctly by locating the actual references, so the looseness cost nothing.

**Claude — process.** Strong throughout. It grounded every claim in file state rather than the ledger prose ("Let me ground the review in the current file state"), verified after each change with the right gate, distinguished `make check` from `make check-strict` correctly, and proved the R8 regression was pre-existing rather than guessing. The provenance refusal (insight 1) is exemplary judgment. Minor friction: two avoidable Edit-before-Read failures (version-data.yaml line 18, version-record.md table) cost a round-trip each, though they were recovered immediately and the tool feedback made them cheap. It also correctly used `make render` to propagate the quarantined value and confirmed the unit diffs were *only* the stamp.

**Claude — communication.** Clear, structured, appropriately leveled. The open-loops review and the R8 root-cause writeup are model explanations: root cause, why only R8 fell through, why nobody caught it, and ranked fix options with a recommendation and rationale for rejecting the alternatives. It consistently separated the in-scope work from incidental findings ("⚠️ One separate finding (pre-existing, NOT from this refresh)") so the human could triage. It surfaced judgment calls (per-key dates; pre-commit asymmetry) instead of burying them. Commit messages were thorough and accurate. If anything, the summaries occasionally run long, but the density matches a maintainer audience.

**What didn't (either party).** Very little. The Edit-before-Read stumbles are the only real process wart, and they were inconsequential. There is a latent point worth noting: the human left the pre-commit/CI hooks non-strict by accepting Claude's framing — meaning the same class of silent regression (a PEND that should be a FAIL slipping past plain `make check` at commit time) could in principle recur until someone runs `close-unit` or strict manually. Claude flagged exactly this and offered to move enforcement into the hook; the human did not take it up in this session. That is a reasonable deferral, not an error, but it is the one residual exposure.

## Bottom line

A high-functioning maintenance session whose through-line is *honest verification over convenient completion*. The nominal task (disarm a deliberately-held version trigger) was small; the value came from Claude refusing to fabricate per-key provenance, then turning an incidental strict-gate failure into a root-caused, systemically-fixed regression — and from the human supplying the one unrecoverable piece of context at the right moment and pushing the fix from symptom to class. Both parties operated with disciplined per-change approval and file-grounded claims. The only blemishes are two trivial Edit-before-Read retries and a consciously-deferred hardening of the pre-commit hook.
