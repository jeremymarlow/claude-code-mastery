---
session: 2026-05-30_1703-u13-subagents-authoring
reviewer: outcome-auditor
model_evaluated: "claude-opus-4-8"
grades:
  human:   { process: did-well, communication: did-well }
  claude:  { process: did-well, communication: did-well }
  overall: did-well
---
## Insights (ordered by significance)

1. **The session's headline artifact — the U14 dogfood hook — is genuinely sound, not "done" theater.** `tools/check-on-edit` was written, then exercised against the four payload classes that matter: a course-file edit on a green tree (silent, exit 0), an unrelated file (no-op), a no-`file_path` Bash payload (no-op), and a deliberately-broken throwaway unit that correctly produced `decision: "block"` with the failing output in `reason`, followed by cleanup confirming the tree went green again (_Evidence:_ "🤖 Assistant" turns at lines 5111–5161). I independently read the on-disk script: it matches every claim — thin wrapper, path-gated to `course/`/`meta/`, runs `make check`, emits `decision:"block"` on failure, swallows unparseable stdin (_Evidence:_ `/home/jj/dev/claude-training/tools/check-on-edit` lines 24–56). The `decision:"block"` field is the schema-correct choice for a `PostToolUse` hook (the skill's own reference at line 1911 confirms `block` is valid there, deprecated only for `PreToolUse`). This is the rare case where the conversation's claim ("Pipe-tested all paths; jq -e validated") is fully backed by the delivered file. [claude] [process]

2. **The U13 unit prose is correct and carefully hedged on exactly the facts it couldn't fully verify.** The version-specific surface (`--agent` / `--agents <json>` / `agents` subcommand) was re-confirmed against live `claude --help` before authoring (_Evidence:_ lines 491–541), and the parts that `--help` does *not* establish — the `.claude/agents/` on-disk path and front-matter fields, and the `tools` array in the worked-example JSON — are explicitly flagged as "filesystem **convention** — confirm against the docs" rather than asserted (_Evidence:_ `unit.md` lines 113–117, 138–140). That is the right epistemic posture for a version-resilient course, and it means the artifact won't ship a fabricated schema detail as fact. The "subagent vs. skill," "three reasons to delegate," and "delegation is trust delegation → verify" spine is internally consistent and ties cleanly to U3/U12/U15. [claude] [process]

3. **Every "green" claim in this session is anchored to a real check run, and the residual PENDs are honestly characterized.** `make check` was run after each unit and the persistent PENDs (`R8`, `C15–C17` labs, the rubric dimensions) were each correctly attributed to downstream work (U14–U16 labs, P6 capstone) rather than waved away (_Evidence:_ close-out summary at lines 1226, 1357, 5395). The C14→absent and C15→present transitions in the traceability output were noted as positive evidence the new units actually wired in (_Evidence:_ line 5404 "C15 now practiced by a lab; only C16/C17 remain"). The U16 commit is candid that `ci` and `checkpoint-rewind` "stay L1-unverified (external Action / in-REPL /rewind)" instead of claiming false completion (_Evidence:_ commit body lines 7239–7241). An independent checker re-running `make check` would land on the same green-with-expected-PENDs state. [claude] [communication]

4. **The one real soft spot is that all four units land a no-refs "prose-self-check" lab — defensible, but it means none of these units ships an independently-verifiable lab artifact.** U13, U14, U15, U16 each follow the U8/U10/U12 precedent of a checklist self-check with no `start/` tag, `solution/` branch, or `verify.sh` (_Evidence:_ U13 lab block `unit.md` lines 163–217; close-out line 5585 "no `start/`/`solution/` refs … the self-check is an **objective pipe-test**"). For U14 the "objective pipe-test" framing is strong because the hook *is* a runnable artifact a learner can drive. For U13/U15/U16 the self-check leans on learner judgment ("can you articulate why this was a subagent"). That is a legitimate design call given the lab subjects (the artifact lives in the learner's own `.claude/`), and the L7 ledger tracks it correctly rather than hiding it — but it does mean four consecutive units carry no machine-verified lab, and an auditor should note the course's verification rigor here rests on prose checklists, not green tests. Claude was transparent about this throughout, which is why it doesn't drag the grade down. [claude] [process]

5. **Scope honesty: this is a four-unit, P5-completing session mislabeled by its slug as a single U13 build.** The slug and title say "U13 subagents authoring," but the human drove it straight through U14, U15, and U16 to close P5 (_Evidence:_ user turns "commit and push this, and start U14" line 1365, "commit and push, then start U15" line 5600, "commit and push U16" line 7217). The work product held up across all four — each got the same prime→author→verify→close-unit→commit discipline — so the breadth didn't dilute quality. But a reviewer relying on the slug would under-count what shipped; the artifacts, not the title, are the truth here. [human] [process]

6. **The human's per-change authorization and the branch-naming detour both improved the outcome.** The human gave explicit, separate go-aheads for each commit/push rather than a standing blanket (_Evidence:_ the three distinct "commit and push …" turns), honoring the CLAUDE.md per-change rule. The branch-naming question at line 7217 drew a genuinely useful answer: Claude checked the *actual* branch history (`git branch -a`, `meta/conventions.md`) instead of reciting the Kiro default, surfaced that "Kiro has exactly three spec phases and tasks is the last," and recommended against a misleading `spec/finalize-phase` — then the human's instinct to "keep it … since we are still technically in the tasks phase" was correctly affirmed with a sound rationale (P6 is the last *slice* of tasks, gated on `check-strict`) (_Evidence:_ lines 7355–7398). This is a small but real quality save: it prevented a misnamed branch and a premature merge of a half-finished phase. [human] [communication]

## What worked / what didn't (both parties)

**Claude — strengths.** The standout is artifact discipline on the U14 hook: it didn't trust the skill-provided schema blindly, it built a thin wrapper (policy stays in the existing `make check` suite, no duplication), pipe-tested every branch including the failure path with cleanup, and `jq -e`-validated the merged `settings.json` while confirming the 12 existing permissions survived the merge (_Evidence:_ lines 5232–5247). It correctly anticipated the settings-watcher caveat — that the hook likely won't auto-fire this session because `.claude/` had no `hooks` block at session start — and named the pipe-test, not in-session firing, as the real proof (_Evidence:_ lines 5376, 5583). Across all four units it verified version surface against live `--help` before authoring and kept the L1/L2/L7 ledgers honest. The U13 prose is teaching-grade and well cross-linked.

**Claude — weaknesses.** Minor: the rendered Edit diffs collapse to identical old/new in several places (e.g. lines 619–625, 5210–5211), so a few small edits aren't independently visible in the render — not a defect in the work, just a limit on what I could verify from prose alone (I cross-checked the on-disk hook and unit to compensate). The four-in-a-row no-refs labs, while defensible, are the only place where "verified" rests on checklist judgment rather than an executable artifact for three of the four units.

**Human — strengths.** Kept the session moving with crisp, per-change authorizations that respected the push-discipline rules, and asked the branch question at exactly the right moment — before merging — turning a naming nit into a correct decision about phase boundaries. Trusted the dogfooding instinct (wire the real hook) that produced the session's best artifact.

**Human — weaknesses.** Essentially none on the outcome axis. The only thing worth flagging is that the lab-rigor pattern (no executable verifier for U13/U15/U16) was accepted without push-back; a stricter author might have asked for at least one machine-checkable lab among the four. Given the artifacts genuinely live in learner config, this is a judgment call, not a miss.

## Bottom line

A high-output session that finished P5 (four units) while keeping the work product sound: the U14 hook is a real, rigorously-tested dogfood artifact whose on-disk form matches every claim made about it, the U13 prose is correct and honestly hedged on unverified schema details, and every "green" was tied to an actual check run with PENDs accurately attributed to downstream work. The single caveat an independent auditor should carry forward is that four consecutive units ship no-refs prose-self-check labs — defensible and transparently ledgered, but it concentrates the course's lab-verification rigor in checklists rather than executable tests. The slug undersells the session; the artifacts oversell nothing.
