---
session: 2026-05-31_1730-memory-uniformity-and-auto-memory-disable
reviewer: outcome-auditor
model_evaluated: "claude-opus-4-8"
grades:
  human:   { process: did-well, communication: did-well }
  claude:  { process: did-okay, communication: did-well }
  overall: did-okay
---
## Insights (ordered by significance)

1. **The load-bearing artifact came out correct and verified — `autoMemoryEnabled: false` is sound JSON, scoped right, with non-fabricated provenance.** The session's central deliverable is a one-line config change, and it holds up to independent check: the committed `.claude/settings.json` is valid JSON (`autoMemoryEnabled = False`, top-level keys `['autoMemoryEnabled','permissions','hooks']`), permissions and hooks intact exactly as claimed, and `make check` passed before commit (the `R8` PEND is the known non-strict placeholder, unrelated). Crucially, the setting name was **not** authored from memory: Claude dispatched the `claude-code-guide` subagent with an explicit "do NOT answer from training memory" instruction and surfaced the provenance (code.claude.com/docs/en/memory.md). For a version-specific value, this is precisely the discipline the repo's own working agreement demands — the artifact is verified-by-construction, not asserted. _Evidence:_ "→ Agent … claude-code-guide" turn; the post-edit `python3 -c "import json…"` check returning `valid JSON`; the final `git --no-pager diff` showing only the additive `+ "autoMemoryEnabled": false,` line. [claude] [process]

2. **A real defect shipped in the maintainer-guide note: the "gitignored `.claude/settings.local.json`" escape hatch is not actually gitignored in this repo.** The committed note tells a maintainer to "set `"autoMemoryEnabled": true` in a gitignored `.claude/settings.local.json`" to re-enable memory locally without committing. But the repo's `.gitignore` contains no entry for `settings.local.json` (it lists only spec scratch, Python, and editor patterns). Claude Code does treat `*.local.json` as local by convention, but this repo never adds the ignore rule, so a maintainer who follows the instruction literally and runs `git add .` could commit their personal override — exactly the cross-machine divergence the whole session set out to eliminate. The claim "(gitignored …)" is stated as fact in a delivered learner/maintainer artifact and does not match the delivered repo state. An independent checker reading the note and then `cat .gitignore` finds the mismatch immediately. _Evidence:_ `course/maintainer-guide.md` "## Why this repo runs with auto-memory off" — "a gitignored `.claude/settings.local.json` (local scope wins over project scope)"; `.gitignore` has no `settings.local.json` line. [claude] [process]

3. **The memory audit's analysis was accurate and its edits were correct, but the migration left a small loose end.** The audit findings were genuinely right: `MEMORY.md` did claim "U1–U12 done" three phases stale, and `project-claude-code-mastery.md`'s "Live status" paragraph both was stale (referenced the now-deleted `spec/tasks-phase`) and violated the repo's own no-duplicate-live-status rule. The rewrites are sound, and the `R1–R13`→generalized "requirement IDs" fix in `feedback-spec-driven-honor-gates.md` correctly pre-empts rot (the set is now R1–R17). However, the final summary claimed bucket-A facts were "fully captured in committed files" and then emptied the memory dir — yet the user-global `~/.claude/CLAUDE.md` that the two bucket-B items (small-tool-batches, read-over-shell) were migrated into **does not exist on disk now**. That file is outside the repo and outside this session's later control, so this isn't a confirmed in-session loss, but it means the one durable place those two hard-won lessons lived is unverifiable post-session — and the session deleted the originals. The auditor cannot confirm the migration's persistence held. _Evidence:_ "→ Write `/home/jj/.claude/CLAUDE.md` (write 1685 chars)"; "→ Bash … `rm -f *.md`"; `~/.claude/CLAUDE.md` absent at review time. [claude] [process]

4. **Process theater of the wasted-call variety: ~10 duplicate Read calls and two batch-race greps — the very mistakes a memory file the session was auditing documents.** During the memory read, Claude fired the entire 8-file Read batch twice; the second set all returned "Wasted call — file unchanged." It then ran a dangling-reference `grep` in the same batch as the deletes/writes it depended on, got a stale answer, and had to re-run. None of this corrupted the *output* — the final state is correct — but it is exactly the failure `feedback-small-tool-batches.md` warns against, on the same turn Claude was reading that file. Claude caught and named both lapses honestly ("I duplicated that batch — exactly the … mistake"), which is to its credit for transparency, but the auditor's lens is the work product *and* whether process noise masked anything; here the noise was harmless to the artifact but is a recurring, self-diagnosed defect in execution. _Evidence:_ lines of repeated "Wasted call — file unchanged since your last Read"; "Note to self: I duplicated that batch"; "The grep ran against pre-write state (parallel batch race)". [claude] [process]

5. **The strategy answer was substantive and correct, not hand-wavy — the bucket A/B/C audience model is the right frame and the dotfiles gap was identified unprompted.** When the human asked whether "memory off, everything to CLAUDE.md" is a common pattern, Claude gave a genuinely sound analysis: it corrected the over-blunt "all into CLAUDE.md" by sorting facts by audience (project→committed, personal-cross-project→user-global, machine-local→drop), mapped each existing memory file into a bucket, and flagged the real residual gap (user-global `~/.claude/CLAUDE.md` is itself per-machine, needing dotfiles to truly close the loop). This is correct as strategy and would hold up to an independent reviewer. It also correctly refused to assert the disable mechanism from memory before verifying. _Evidence:_ "## The nuance: sort by audience" with the bucket table; "## The one gap to plan around — a dotfiles repo you clone on each machine". [claude] [comms]

6. **Strong proactive risk-surfacing on both the stale branch and the collaborator/teaching tension — these protected the work product.** On `quality-pass-p7`, Claude refused the unsafe `-d`, diffed it against `main`, and correctly diagnosed it as superseded WIP whose restoration would *regress* `main` (removing all of P8) — a genuine outcome-protecting check rather than a blind force-delete. Later it flagged that committing `autoMemoryEnabled: false` at project scope forces memory off for any learner who clones, while U4 teaches memory as a feature — a real tension the user had accepted, surfaced with two concrete mitigations. This is the auditor's ideal: catching where a "done" change could quietly damage the product. _Evidence:_ "→ Bash `git log --oneline main..quality-pass-p7`" then "it would **remove** all of P8 … Net: 33k insertions / 114k deletions — it's the *old* world"; "## Two things to flag (b) The collaborator/teaching caveat you accepted". [claude] [comms]

7. **The human steered tightly and made the consequential calls — including the one that produced the shipped defect's context.** The human ran a clean incremental cleanup (merge → switch → delete merged → escalate to `-D` only after seeing the diagnosis), framed the memory-uniformity question well, and chose "Project scope (committed)" for the disable knowing the collaborator caveat — a defensible, deliberately-accepted trade-off. The "leave as-is and add the note … commit and /capture-session" close was crisp. The human also explicitly cautioned "Be careful not to batch race conditions again," directly targeting Claude's known weak spot. The only thing an outcome auditor would note: the human accepted Claude's summaries (including the "fully captured" / migration claims and the gitignore escape-hatch) without an independent spot-check, which is reasonable delegation but is how the `.gitignore` mismatch reached commit unchallenged. _Evidence:_ "delete it with -D"; "Project scope (committed)" selection; "Be careful not to batch race conditions again"; "Lets leave as-is and add the note to the maintainers guide … commit and /capture-session". [human] [process]

## What worked / what didn't (both parties)

**Claude — worked:** The core artifacts that matter are correct. The committed settings change is valid, scoped, and verified-by-construction with honest provenance (subagent, not memory). The memory audit's diagnosis was accurate and the rewrites fixed real staleness without introducing new errors there. The git-branch cleanup was careful where it counted (the `-D` diagnosis genuinely protected `main`). The strategy answer was substantive and the bucket model is sound. Risk-surfacing was excellent — the collaborator/teaching tension and the stale-branch regression were both caught proactively.

**Claude — didn't:** One real defect shipped — the maintainer-guide's "gitignored `.claude/settings.local.json`" claim does not match this repo's actual `.gitignore`, so a maintainer following it could commit the override the whole session aimed to prevent. The bucket-B migration target (`~/.claude/CLAUDE.md`) is unverifiable post-session while the originals were deleted, so "nothing lost" is a claim the auditor can't confirm. And the duplicate-Read / batch-race execution noise recurred on the very turn Claude was auditing the memory that documents it — harmless to the final artifact, but a persistent self-diagnosed process flaw.

**Human — worked:** Tight, sequential steering; good problem framing; consequential calls (the `-D` delete, project-scope disable, the maintainer-note close) all defensible and clearly communicated. Explicitly pre-empted Claude's known batch-race weakness.

**Human — didn't:** Accepted Claude's wrap-up summaries — including the gitignore escape-hatch and the "fully captured / nothing lost" migration claim — at face value; a light spot-check would have caught the `.gitignore` mismatch before it was committed.

## Bottom line

A small-surface, config-and-docs session whose load-bearing artifact (the committed `autoMemoryEnabled: false`) came out correct, valid, and properly verified rather than asserted from memory — and whose strategy and risk-surfacing were genuinely strong. The through-line is "right thesis, right central change, sound reasoning," dragged from did-well to did-okay by two outcome blemishes: a documentation claim that doesn't match the delivered repo (the non-gitignored `settings.local.json` escape hatch shipped in the maintainer guide) and a migration whose persistence the auditor can't confirm while its originals were deleted — plus a recurrence of the duplicate-batch noise the session was simultaneously auditing. The result holds up to independent check on the part that matters most, but not on the prose around it.
