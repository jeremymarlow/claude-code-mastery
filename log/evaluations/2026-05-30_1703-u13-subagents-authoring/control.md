---
session: 2026-05-30_1703-u13-subagents-authoring
reviewer: control
model_evaluated: "claude-opus-4-8"
grades:
  human:   { process: did-well, communication: did-okay }
  claude:  { process: did-well, communication: did-well }
  overall: did-well
---
## Insights (ordered by significance)

1. **The session's title and slug name U13, but it actually authored four units (U13–U16) plus a real dogfooded hook and MCP fixture — a marathon that completed all of P5.** What looks like a single-unit slice became the close of the entire authoring phase. The throughput is real and the quality held across all four, but it directly contradicts the project's own context-management protocol: `tasks/P5-units.md` opens with "author **one unit per session/slice**, loading only that unit's row… not the other units" (transcript L279-282), and the memory pointer repeats "Work one unit per slice (context protocol)" (L1497). Neither party paused to name that the slice had grown 4×. Context never became a problem (ended at 22%, L6518), so the protocol's *cost* never bit — but the discipline it encodes was quietly set aside without comment. _Evidence:_ four `commit and push, then start U{N}` cycles (L1365, L5600, L6573, L7217) inside one session against the one-unit-per-slice rule (L279). [human|process] [claude|process]

2. **Claude's version-currency discipline was the standout: it never authored a CLI fact from memory, and it visibly chose verification over assertion at every version-specific step.** For U13 it re-ran `claude --help` and `claude agents --help` live before writing (L491-541); for U14 it recognized `--help` does *not* surface hook event names, invoked the `update-config` skill to get the authoritative `settings.json` schema, and anchored the unit on the verified event enum (L1599-1631, L5008-5013); for U15 it proved the MCP handshake with `claude mcp add … && claude mcp get` reporting `✓ Connected` rather than claiming it (L6498). This is exactly the behavior `CLAUDE.md` demands ("Never author a version-specific value from memory"), executed without prompting. _Evidence:_ "the `hooks` vd key's `notes` explicitly say to verify event names before hardcoding… let me use it" (L1631). [claude|process]

3. **Claude correctly distinguished a genuine design fork from a routine call and escalated it via AskUserQuestion — instead of quietly picking one.** For U15's offline MCP target it laid out three real options (ship a stdio server / `mcp serve` / conceptual), named the trade-off explicitly ("this is a real design fork that changes what I build… I want your call before building one", L5862), and tied it to a precedent the human had weighed before (the U12 prop swap). The two-question AskUserQuestion (L5866-5898) was well-scoped and the answers fed straight into the build. This is good judgment about *which* decisions are the human's to make. _Evidence:_ L5860-5904. [claude|process]

4. **The dogfooded hook (U14) is the session's most valuable artifact, and Claude built it with real engineering rigor — pipe-testing every path before wiring it.** It wrote `tools/check-on-edit`, then tested green/unrelated/no-path cases (all silent, exit 0, L5113-5128) *and* the failure path by creating a throwaway broken unit to confirm `decision:"block"` fired, then cleaned up and re-confirmed green (L5142-5161). It timed `make check` at 0.18s to justify running it on every edit (L5038). This closed open-loop L2 with a CLI-verified schema, exactly as the spec intended. _Evidence:_ L5085-5165. [claude|process]

5. **The human's instructions were terse and effective but leaned on standing trust in a way that surfaces a minor risk.** "go ahead with U13" (L260), "commit and push this, and start U14" (L1365), "commit and push, then start U15/U16" — each `push` was an explicit per-change authorization, so Claude's pushes were compliant with the "never push without confirmation" rule. But the human gave no scope ceiling, no "stop after U13," and never flagged the growing slice; the rhythm of "commit and push, then start the next" is precisely the "commit-per-slice rhythm" `CLAUDE.md` warns against treating as blanket permission. Claude stayed disciplined (it stopped and presented U15 before pushing, L6511), so no harm resulted — but the human's open-ended chaining is what enabled the four-unit drift. _Evidence:_ L1365, L5600, L6573 each pairing a push with "start U{next}". [human|communication]

6. **The `/close-unit` double-fire was handled gracefully — Claude verified rather than duplicated.** After it had already run every close-unit step inline at the end of the U13 turn, the human fired `/close-unit` with no argument (L1253). Claude recognized the work was done, re-verified each state file was in sync instead of writing duplicate ledger entries, and said so plainly (L1282, L1349). Good idempotency instinct under an ambiguous re-trigger. _Evidence:_ L1282-1359. [claude|process]

7. **The closing branch-naming exchange showed Claude checking reality over reciting a default — and then correcting course when the human pushed back.** Asked for the next branch name "following Kiro nomenclature," Claude inspected actual branch history (L7309-7345), surfaced the honest wrinkle that Kiro has no canonical 4th phase, and recommended `spec/finalize-phase`. When the human countered "I should keep it then since we're still technically in the tasks phase, right?" (L7385), Claude agreed crisply and grounded it in the project's own merge-on-gate pattern (L7389-7398) rather than defending its prior suggestion. _Evidence:_ L7357-7398. [claude|communication]

## What worked / what didn't (both parties)

**Claude — process (did-well).** The strongest dimension of the session. Every version-specific claim was verified against the live CLI before it landed in prose, including the hard case where `--help` was insufficient and Claude reached for the right skill to get the authoritative schema. It dogfooded two genuinely-used artifacts (the check-on-edit hook closing L2; a working offline MCP server with a proven handshake), pipe-tested them, and cleaned up its scaffolding. It tracked traceability state file-by-file, kept `make check` green throughout, and watched the right open loops (L1, L2, L7). It escalated the one real design fork rather than guessing. The only blemish is the unflagged scope expansion: the project's own protocol says one unit per slice, and Claude rode four `start U{next}` instructions without ever noting that the session had outgrown the protocol or asking whether to checkpoint. It got away with it because context stayed low — but a fresh-session reader of the IMPLEMENTATION dashboard would not have predicted a four-unit jump from a slice that prudence says to keep small.

**Claude — communication (did-well).** End-of-unit summaries were consistently well-structured: what landed, state synced, open loops, and a framed next-action question. It was candid about nuance (the Kiro "no 4th phase" wrinkle; "if the handshake won't verify, I flag it rather than fake it", L5878) and never claimed status it hadn't earned ("No fabricated status," L1359). It reversed cleanly when the human corrected the branch call. Prose was dense but navigable.

**Human — process (did-well).** The human ran a clean spec-driven loop: opened with `/prime-context`, approved work in small verbal increments, gave explicit per-push authorization each time, and asked a genuinely good governance question at the end (branch naming + merge timing). The decisions delegated to Claude were well-chosen, and the AskUserQuestion was answered decisively. The one weakness shared with Claude is that the human set no scope ceiling and let the slice run to four units without comment, which is at odds with the protocol the human's own repo encodes.

**Human — communication (did-okay).** Instructions were efficient and unambiguous in intent, and the closing question was sharp. But they were minimal and never named scope — "start U14… start U15… start U16" silently turned a U13 session into a P5-closing marathon. Brief is fine; a one-line "let's keep going through P5 this session, watch context" would have made the expanded scope a deliberate joint decision rather than an emergent one, and would have made the transcript's title honest.

## Bottom line

A high-functioning collaboration that quietly broke its own one-unit-per-slice rule and got away with it. Claude's verification discipline, dogfooding rigor, and judgment about when to escalate were genuinely strong, and the human steered with efficient, well-placed approvals — together they closed all of P5 with `make check` green and two real, CLI-verified artifacts (the check-on-edit hook and the offline MCP server). The one through-line worth flagging is scope: a session named for U13 authored U13–U16, and neither party paused to acknowledge that the slice had quadrupled past the protocol the repo itself teaches. No damage done this time because context never tightened — but the discipline of naming the expansion, not the expansion itself, is the gap.
