#!/usr/bin/env python3
"""check-version-drift (R12.AC6): make a CLI change tool-prompted instead of memory-dependent.

Compares the installed `claude --version` to the recorded verified version
(meta/version-data.yaml `_verified_version`, mirrored in meta/version-record.md), and — where
feasible — diffs the installed `claude --help` command list against the committed CLI reference
(meta/cli-reference.json, R16) to surface new / removed / renamed commands that may need coverage.
The reference is the single recorded command surface (it folds in the former cli-commands.snapshot,
L10); the cheap top-level diff here complements the full `render-cli-reference --check --cli`
re-introspection (see the `drift` make target).

Drift is informational by default (exit 0) so it never blocks day-to-day work; pass `--strict`
(used by the scheduled refresh trigger) to exit non-zero when drift is detected. This check is
itself the worked example U16 references for "Claude in CI / on a schedule" (R14.AC2).
"""
import json
import os
import subprocess
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from _common import (  # noqa: E402
    META, Reporter, cli_version, load_version_data, parse_help, run_help,
)

CLI_REFERENCE = META / "cli-reference.json"


def recorded_commands():
    """Top-level command names from the committed CLI reference (R16) — the recorded surface the
    live `claude --help` is diffed against. Generated by the same `parse_help` introspection, so the
    names are already canonicalised ("plugin|plugins" -> "plugin") and align with installed_commands()."""
    ref = json.loads(CLI_REFERENCE.read_text())
    return {c["name"] for c in ref["root"]["commands"]}


def installed_version():
    return cli_version()


def installed_commands():
    # The shared help parser (R16) yields the top-level command list; it already canonicalises
    # alternates ("plugin|plugins" -> "plugin") and skips embedded example blocks.
    return {c["name"] for c in parse_help(run_help([]))["commands"]}


def main(argv):
    strict = "--strict" in argv
    rep = Reporter("check-version-drift")
    recorded = load_version_data().get("_verified_version")

    try:
        installed = installed_version()
    except (FileNotFoundError, subprocess.SubprocessError) as exc:
        rep.pending(f"could not run `claude --version` ({exc}); skipping drift check")
        return rep.done(strict=False)

    if installed is None:
        rep.pending("could not parse installed CLI version; skipping")
        return rep.done(strict=False)

    if installed == recorded:
        rep.ok(f"installed CLI {installed} matches recorded verified version")
    else:
        rep.pending(f"DRIFT: installed {installed} != recorded {recorded} — re-verify "
                    f"version-specific facts and bump meta/version-record.md (R12.AC7)")

    # command-list diff (best effort) vs the committed CLI reference (R16)
    if CLI_REFERENCE.exists():
        try:
            recorded_cmds = recorded_commands()
        except (OSError, ValueError, KeyError) as exc:
            rep.pending(f"could not read {CLI_REFERENCE.name} ({exc})")
        else:
            try:
                now = installed_commands()
                added, removed = sorted(now - recorded_cmds), sorted(recorded_cmds - now)
                if added:
                    rep.pending(f"new CLI command(s) since the recorded reference: {added} — "
                                f"re-run `tools/render-cli-reference --all`")
                if removed:
                    rep.pending(f"CLI command(s) removed since the recorded reference: {removed} — "
                                f"re-run `tools/render-cli-reference --all`")
                if not added and not removed:
                    rep.ok(f"command list unchanged vs cli-reference.json ({len(now)} commands)")
            except (FileNotFoundError, subprocess.SubprocessError) as exc:
                rep.pending(f"could not diff command list ({exc})")
    else:
        rep.pending(f"no CLI reference at {CLI_REFERENCE.name}; run `tools/render-cli-reference --generate`")

    return rep.done(strict=strict)


if __name__ == "__main__":
    raise SystemExit(main(sys.argv[1:]))
