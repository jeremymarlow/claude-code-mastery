---
session: 2026-05-30_1930-p6-capstone-finalization
reviewer: outcome-auditor
model_evaluated: "claude-opus-4-8"
grades:
  human:   { process: did-well, communication: did-well }
  claude:  { process: did-well, communication: did-okay }
  overall: did-well
---
## Insights (ordered by significance)

1. **The headline artifact — `tools/render-checklist` — was proven correct against the live tree, not merely asserted.** Claude found that `course/progress-checklist.md` carried a `<!-- GENERATED ... Regenerate with the capability-map generator (P3 tooling) -->` header while **no such generator existed** — a false claim shipped in a learner-facing artifact. Rather than paper over it, it built the real generator and then *falsified its own work*: ran it and diffed against the committed file. The diff came back one line (`1 file changed, 1 insertion(+), 1 deletion(-)`), exactly the corrected header line. _Evidence:_ "→ Bash `chmod +x tools/render-checklist && … git diff course/progress-checklist.md`" returning the single-hunk diff; turn "The generator reproduces the file byte-for-byte except the one header line I deliberately corrected." This is the gold standard for outcome auditing: the claim ("regenerates from the map", task 6.4 / R9.AC5) is now *mechanically true and demonstrated*, not hand-waved. I independently confirmed the shipped tool handles the cross-cutting stage (`if any(c["stage"] == "cross-cutting" …)`) and carries a `--check` drift mode wired into `make check` — so the artifact also guards itself going forward. [claude] [process]

2. **The rubric — the load-bearing deliverable — is correct and complete, confirmed by an independent gate.** R8.AC3 demands the rubric's dimensions collectively cover *every* can-do; the mechanical proof is `check-traceability --strict` flipping `['C1'…'CV']` from PEND to PASS. Claude wrote `rubric.md` and then ran the strict check, which reported "every can-do is assessed by >=1 capstone-rubric dimension." _Evidence:_ "→ Bash `tools/check-traceability --strict`" → three PASS lines. Reading the as-shipped `course/capstone/rubric.md`, the content also satisfies the *qualitative* half the gate can't see: it grades the **work product** at demonstrated/partial/not-yet (R8.AC3), is explicitly self-applicable (R8.AC4), and routes the CV reflection (one catch / one justified accept / one override, R8.AC6) — none of which the regex enforces, but all of which are present and sound. The artifact matches what was claimed about it. [claude] [process]

3. **The three briefs are concrete and each genuinely meets R8.AC1, with every referenced path verified to exist before linking.** Before authoring, Claude probed `codebases/fixtures/taskflow_mcp.py`, the two codebase dirs, etc. ("→ Bash `for p in … do [ -e "$p" ] …`" → all OK), so the links in `briefs.md` resolve — confirmed by `check-links: OK`. Each brief (A: spec-first feature + custom command; B: legacy refactor + subagent + hook; C: MCP integrate + vet) independently requires context engineering + ≥1 extension + a non-trivial workflow + explicit verification, the four R8.AC1 obligations. The "Done when" clauses are objective (suite green, ownership/404 boundary holds, behavior-equivalence proven, vetting verdict written), not vibes. This is fit-for-purpose output. [claude] [process]

4. **Claim slightly outran evidence at the finish: "all 11 Definition-of-Done items hold."** The closing summary asserts "all 11 Definition-of-Done items hold" and "The v1 build is complete." But what was *actually* verified is the **mechanical** gate — `make check-strict` green (front-matter, coverage, links, version-refs, traceability) plus the drift check. The DoD per `IMPLEMENTATION.md` §6 includes content-quality items the static suite cannot see (e.g. tier-appropriate coverage, authentic self-examples, prose quality). Earlier in the session Claude was admirably precise about this very distinction — "the content obligations … that the static suite can't see but the DoD requires" — yet the final claim collapses mechanical-green into all-eleven-hold without re-walking the eleven. _Evidence:_ final turn "all 11 Definition-of-Done items hold" vs. the earlier turn distinguishing mechanical from content obligations. The work is very likely sound, but the *claim* asserts an audit it didn't visibly perform. A reader trusting that sentence would believe more was checked than was. [claude] [comms]

5. **The "no blank page" exemplar dependency was sequenced and handled correctly under the forward-link hook.** Claude authored the case study first (the R8.AC2/R14.AC4 exemplar) so the briefs could point at it, and when the pre-edit hook flagged a forward reference to the not-yet-existent `maintainer-guide.md`, it down-converted the link to plain text, then re-promoted it to a real link once the target existed. _Evidence:_ "The hook caught a forward reference — `maintainer-guide.md` doesn't exist yet" → edit to plain text → later "convert the case study's earlier plain-text maintainer-guide reference into a proper link." The end state has no dangling links (confirmed by `check-links: OK` in the pre-commit run). Process detail that protects the artifact. [claude] [process]

6. **The human's one interjection was a real correctness/efficiency catch.** When Claude tried `source /home/jj/venvs/main/bin/activate`, the human rejected it: "you shouldn't need to source the venv, it's laready active." Beyond saving a step, this prevented Claude from activating the *wrong* venv (`/home/jj/venvs/main` vs. the project's `.venv`), which could have masked or skewed the very gate the session hinges on. _Evidence:_ tool-rejection turn. Terse, accurate, outcome-protecting steering. [human] [comms]

7. **The PR explanation was honest about environment reality rather than reciting a default that would fail.** Asked to explain the PR mechanism, Claude first checked the remote (`gitea.marlow.home`, not GitHub) and probed for `gh`/`tea`/`glab`/`curl`, then explicitly flagged that `gh pr create` "would fail against a non-GitHub remote" and is not installed. _Evidence:_ "→ Bash `for t in gh tea glab curl; …`" and "What I would *not* do: reach for `gh pr create`." This is outcome-honest: it didn't hand the user a confident-but-broken recipe. Minor unverified-claim risk in the `curl` API path (the exact Gitea endpoint was described from knowledge, not tested), but it was correctly framed as needing a token and user confirmation, so nothing untested was *run*. [claude] [comms]

## What worked / what didn't (both parties)

**Claude — strengths.** This is a strong outcome session. The defining moment is the checklist generator: Claude treated a stale "GENERATED" header as a shipped defect (R14.AC3 authenticity) and resolved it by making the claim *true*, then proved it with a byte-level diff. That is exactly the disposition this lens prizes — correct, claim-matching output over a tidy "done." The rubric and briefs are complete and faithful to R8, and crucially Claude leaned on an *independent* mechanical gate (`check-traceability --strict`) to prove coverage rather than self-attesting. Sequencing was disciplined (exemplar → rubric/briefs → maintainer guide → README → state sync), forward-link hazards were handled cleanly, and the version stamp was actually re-verified against the installed CLI (2.1.158, drift green) instead of assumed. State sync across four files plus the open-loops ledger (striking L3, L5) was thorough and accurate.

**Claude — weaknesses.** The one real fault is the closing over-claim — "all 11 Definition-of-Done items hold" — which asserts a content-level audit the session only partly performed; the *mechanical* gate is what was demonstrated. Given how carefully Claude drew the mechanical-vs-content line mid-session, the final sentence should have stayed within that line. Lesser note: the Gitea `curl` PR snippet was offered from knowledge, not verified, though it was correctly gated behind user confirmation so nothing untested ran.

**Human — strengths.** Light-touch but high-value. The plan was approved through an explicit `AskUserQuestion` fork (briefs), and the venv interjection was a precise correctness catch. The commit/push authorization was clear and per-change ("commit this and push"). Communication was efficient and unambiguous.

**Human — weaknesses.** Essentially none for this session's scope. The user authorized push without independently spot-checking the artifacts first, but the strict gate + pre-commit hook provided an objective backstop, so this was a reasonable delegation rather than a lapse. Note for the record: a P7 session later edited `rubric.md` (its header now references a "P7 de-coding sweep" absent from this session's Write), so the *currently shipped* file differs slightly from what was authored here — not a defect of this session, but a reminder that the as-built artifact is what this review credits.

## Bottom line

A genuinely sound finalization session: the artifacts came out correct, complete, and — most importantly for this lens — *proven* correct against independent gates rather than declared done. The standout is converting a false "GENERATED" header into a real, byte-verified generator; the rubric and briefs are fit for purpose and claim-matching. The only blemish is the final "all 11 DoD items hold" sentence overstating a mechanical-green result as a full content audit. The product holds up to an outside check — which is the whole point.
