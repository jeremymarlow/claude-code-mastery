---
session: 2026-05-30_1930-p6-capstone-finalization
reviewer: control
model_evaluated: "claude-opus-4-8"
grades:
  human:   { process: did-well, communication: did-well }
  claude:  { process: did-well, communication: did-well }
  overall: did-well
---
## Insights (ordered by significance)

1. **Claude caught a false "GENERATED" claim and fixed the root cause rather than the symptom.** The progress-checklist header declared it was generated by "the capability-map generator (P3 tooling)" — but no such generator existed. Claude flagged this as an authenticity violation (R14.AC3) and built the real `tools/render-checklist`, then proved correctness by regenerating: the output reproduced the file byte-for-byte except the one header line it deliberately corrected. It then wired the tool into `make check`/`render` so drift is caught going forward. This is the strongest moment of the session — it converted a documentation lie into an enforced invariant instead of quietly leaving it, or instead of just editing the header text to a different lie. _Evidence:_ "its header claims it's 'GENERATED ... Regenerate with the capability-map generator (P3 tooling)' — and **that generator doesn't exist**. That's a false claim in a shipped artifact." [claude] [process]

2. **The plan led with the exact mechanical finish line before any prose.** Claude ran `make check-strict`, distilled the entire P6 surface to "only **two** PENDINGs remain" (R8 unreferenced; 18 can-dos unassessed by rubric), and separated the mechanical gate from the content obligations the static suite can't see. This grounded the whole session in the source of truth rather than recollection, exactly per the working agreements. _Evidence:_ "The strict gate gives the precise mechanical target: only **two** PENDINGs remain ... Everything else is already green under `--strict`." [claude] [process]

3. **Claude authored files in dependency order to keep the link checker green, and handled forward-references cleanly.** It wrote rubric → briefs → README so each only links existing targets, used plain-text placeholders for not-yet-created targets (the maintainer-guide reference in the case study), then upgraded them to links once the target existed. It also caught and removed literal `{{vd:key}}` tokens that the version-refs check tries to resolve. This shows real understanding of how its own tooling reacts to edits. _Evidence:_ "I'll author in dependency order so each file only links to already-existing targets" and the later edit converting the case-study placeholder back into a link. [claude] [process]

4. **The human's single mid-session correction was sharp and saved noise.** Claude tried to `source` the venv; the human rejected it with "you shouldn't need to source the venv, it's laready active." One terse, correct intervention; Claude adapted immediately and never repeated it. The human otherwise stayed appropriately hands-off, letting a well-grounded plan run. _Evidence:_ rejected tool use: "you shouldn't need to source the venv, it's laready active". [human] [process]

5. **Claude refused to overreach on the PR while still being maximally useful.** Asked to "explain the mechanism you would use to create a pr," Claude first probed reality (`git push` showed a **Gitea** remote, not GitHub), then verified what tooling actually existed (`gh`/`tea`/`glab` all missing; only `curl`) before answering. It explicitly noted `gh pr create` would fail against a non-GitHub remote — and flagged that the course's own U8 teaches `gh` because the course targets GitHub. It treated PR creation as outward-facing and kept the human in the loop (hand back the link / require a token) rather than auto-creating. This is correct handling of the commit/push boundary plus genuine epistemic humility (it checked instead of assuming). _Evidence:_ "this repo's remote is **Gitea** ... the `gh` CLI ... is **GitHub-only and won't talk to Gitea**" [claude] [process]

6. **Approval boundaries were respected exactly.** Claude finished the work, stopped, presented it, and asked "Want me to **commit** this P6 work?" — it did not infer permission from the per-slice rhythm. The human gave an explicit, combined go-ahead ("commit this and push"), and Claude treated that as authorization for those two acts only. This matches the per-change-not-standing rule precisely. [claude] [process]

## What worked / what didn't (both parties)

**Claude — process.** Excellent. It primed from the files, derived the finish line mechanically, sequenced work to keep checks green at every step, ran the strict gate as a live signal rather than a hope, and did the full state-sync chore (IMPLEMENTATION §3, tasks.md, P6-finalize.md, decisions.md, struck L3/L5 in the ledger). The render-checklist insight elevates this from competent execution to genuinely good engineering judgment. The one nit: Step 4's README "edit" and the P6-finalize task-checkoff edits are rendered with identical old/new strings (a render artifact of the collapsed diff), so I can't fully audit those specific edits from the render alone — but the surrounding `make check`/`check-strict` green runs and the final `git status` corroborate that the intended changes landed.

**Claude — communication.** Clear and well-structured throughout. The P6 plan was scannable, separated mechanical from content work, and explicitly called out what it deliberately would *not* do (L1, needing an interactive REPL). It surfaced the one genuine fork (capstone briefs) via `AskUserQuestion` with a useful preview rather than guessing. The closing summary table and the honest "what remains" section made the state legible. The PR explanation was a model of calibrated candor — three ranked options, an explicit "what I would not do," and the reason it hands back rather than acts.

**Human — process.** Light-touch and effective. Gave a clear directive ("Let's start the plan for P6"), made one precise correction, then let a grounded plan execute. The combined "commit this and push, and then explain..." was an efficient way to authorize and learn at once. Nothing to fault.

**Human — communication.** Terse but unambiguous at every turn. The venv correction, the brief-menu selection, and the commit/push/explain instruction were all clear enough that Claude never had to guess. A bit more could have been said (e.g., confirming the render-checklist scope-expansion was wanted), but the human's trust was well-placed and the brevity was appropriate for the work.

## Bottom line

A clean, high-quality closeout session: Claude drove P6 to a green `make check-strict` (the v1 Definition-of-Done gate that had been red by design all build), and along the way turned a latent documentation lie into an enforced, generator-backed invariant. It grounded everything in the files and tooling, respected the commit/push approval boundary exactly, and met an outward-facing PR request with verified, honestly-caveated guidance rather than an assumption-driven action. The human contributed one well-aimed correction and otherwise trusted a plan that earned that trust. The only friction is render-side (a couple of edits aren't independently auditable from the collapsed diff), not substantive.
