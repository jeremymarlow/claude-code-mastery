---
session: 2026-05-30_1316-open-loops-audit-and-u6
reviewer: control
model_evaluated: "claude-opus-4-8"
grades:
  human:   { process: did-well, communication: did-well }
  claude:  { process: did-well, communication: did-well }
  overall: did-well
---
## Insights (ordered by significance)

1. **Claude turned an "audit the ledger" request into real verification, not a rubber-stamp.** Rather than re-asserting that the open-loops ledger was accurate, Claude actively checked each claim against repo/CLI state: it confirmed `claude --version` still matched the recorded baseline (2.1.158), grepped `claude --help` for the disputed feature tokens, verified the `start/`/`solution/` git refs and SEEDED rows existed, and checked the absence of a hooks block. It then distinguished "genuinely can't verify headless" from "excuse" — and even surfaced a real gap: the L1 notes say verify "via `/help` **or official docs**," and the docs path could be attempted now via WebFetch. _Evidence:_ "the 'in-REPL/docs-only, can't verify headless' reason on those keys is accurate, not an excuse"; "One actionable thing worth flagging on L1…I *could* try clearing some of these against official docs via WebFetch right now." [claude] [process]

2. **The human author's running corrections steadily improved Claude's tool discipline, and Claude internalized them durably.** The author rejected three tool calls in sequence — using `sed` to review files, using shell `awk` to read a task file, and a `${PIPESTATUS[0]}` footgun — each with a terse, correct rationale. After the second nudge Claude recognized the pattern and wrote a persistent memory feedback file rather than just apologizing again. _Evidence:_ "This is the second nudge on it this session, so let me record it as durable feedback"; the author's "won't your pipestatus just be the status of tail?" [human] [communication] [claude] [process]

3. **Every lab was verified end-to-end with the right-reason red/green discipline the units themselves teach — dogfooding in the strongest sense.** For U6 and U7 Claude ran the verifier on the clean/buggy tree first (confirming it fails for the *intended* reason — all 6 tasks returned, `overdue: 0`), built the solution, re-ran to green, then confirmed `reset-lab` restored clean. The U7 verifier deliberately checks *display* surfaces too, so a one-site fix of the copy-pasted bug fails — enforcing the unit's "fix every copy" lesson. _Evidence:_ "verify.sh exit: 1 … expected ids {1, 2}, got {1, 2, 3, 4, 5, 6}"; "overdue is flagged at every surface; future/no-due/done excluded." [claude] [process]

4. **Claude correctly recognized non-mechanizable units and applied an established precedent instead of forcing ceremony.** For U8 (Git/PR) it noticed `gh` isn't installed and that R7.AC7 forbids labs requiring external services, then chose the U2/U4 prose-self-check precedent (no `start/`/`solution/` refs, BYO `gh` stretch marked non-verifiable) — and flagged this as "a deliberate choice, not a shortcut" with reasons. _Evidence:_ "U8 follows the U2/U4 precedent: a prose-self-check lab with a sharp objective checklist and no `start/`/`solution/` refs." [claude] [process]

5. **Permission hygiene was exemplary on both sides.** Claude committed per-slice but never pushed without an explicit instruction, and presented each finished slice for review before proceeding — matching the project's "approval is per-change, not standing" rule. The author gave granular go-aheads ("push start/u06-lab1 and solution/u06-lab1," then separately "push the spec/tasks-phase branch as well"). _Evidence:_ "Nothing pushed — … when you're ready I can push … (I'll ask first)." [claude] [process] [human] [communication]

6. **State synchronization across many parallel surfaces was disciplined and self-correcting.** Each unit updated SEEDED.md, decisions.md (per-unit note + L1/L7 ledger rows), IMPLEMENTATION §3, tasks.md, version-record, and the external memory files — the "don't let parallel lists drift" obligation. When a stale-string Edit failed, Claude re-grepped for exact text and recovered cleanly rather than guessing. _Evidence:_ the failed Edit on IMPLEMENTATION §3 ("String to replace not found") followed by a `grep -n` and a corrected Edit. [claude] [process]

## What worked / what didn't (both parties)

**The human author** ran a tight, high-trust session with low-friction correction. The corrections were the standout: each was a single sentence, technically precise, and aimed at a genuine habit (shell-over-Read, PIPESTATUS), not nitpicking. The author also asked the one strategically interesting question of the session — "Should we start with a fresh context for that meatier work?" — which is exactly the kind of context-management judgment the course teaches, and accepted Claude's honest "not required, but reasonable for tidiness" answer without over-steering. Permission instructions were explicit and granular. The only thing left implicit was the author's own rationale for each rejection, but since the rejections were self-explanatory and Claude understood them immediately, that cost nothing. The two duplicated Context-Usage pastes near the end were a harness artifact, not the author.

**Claude** worked at a high level throughout. Process strengths: it genuinely audited rather than rubber-stamped; it verified every lab end-to-end with red-for-the-right-reason rigor; it caught its own `{{vd:` literal-token bug twice (it would trip `check-version-refs`, which it had just read) and reworded cleanly; it built a deliberate pedagogical through-line (U6 builds `overdue` correctly in the API; U7 debugs the legacy CLI getting it wrong). Communication strengths: summaries were structured, honest about what was *not* done (D2/D3 left unfixed deliberately; `test-run` kept unverified under the L1 hold the author confirmed), and consistently named the next step and asked before pushing. The minor demerit is the recurring early reach for shell (`sed`/`awk`) to read files despite project conventions favoring the Read tool — it took two nudges to stick — though Claude's response (a durable memory note) was the right fix and the habit did not recur after.

## Bottom line

A clean, productive session: an open-loops audit that did real verification work followed by three units (U6 TDD, U7 debug, U8 Git/PR) authored, lab-verified end-to-end, state-synced, and pushed only on explicit instruction. The human steered with a few sharp, correct corrections; Claude absorbed them durably, exercised genuine judgment on what can and can't be mechanically verified, and maintained the project's continuity discipline throughout. The through-line is mature collaboration on a well-instrumented spec where both parties trusted the files over memory and verified rather than asserted.
